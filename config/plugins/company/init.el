;;;;|----------------------------------------------------|
;;;;| (company)
;;;;|----------------------------------------------------|
(use-package company
    :init
      (global-company-mode t)
      (setq company-idle-delay 0))
;;;;|----------------------------------------------------|
