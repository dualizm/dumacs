#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.upd.3874830201
  (:use :cl))
(in-package :ros.script.upd.3874830201)

(defun upd ()
  (uiop:run-program "git add ." :output :string)
  (uiop:run-program "git commit -m \"[upd]\"" :output :string)
  (uiop:run-program "git push" :output :string))

(defun main (&rest argv)
  (declare (ignorable argv))
  (upd))
